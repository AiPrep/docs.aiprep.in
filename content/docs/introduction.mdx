---
title: Introduction
description: AIPREP API documentation — integrate AI-powered educational video generation into your application.
---

# Introduction

The AIPREP API lets you generate AI-powered educational videos from natural language queries. Submit a topic, and AIPREP handles script generation, animation, narration, and video rendering — delivering a ready-to-play video URL.

## What You Can Build

- **E-learning platforms** — Generate on-demand explainer videos for any topic.
- **EdTech products** — Add video generation as a feature in your app.
- **Content pipelines** — Automate educational content creation at scale.
- **Study tools** — Let users generate video explanations for their questions.

## How It Works

1. **Send a query** — POST a text prompt to the `/generate` endpoint.
2. **Track progress** — Subscribe to real-time updates via SSE, or poll the job status.
3. **Get the video** — Once complete, use the returned `video_url` to stream or embed the video.

```bash
curl -X POST https://api.aiprep.in/api/v1/generate \
  -H "X-API-Key: your-api-key" \
  -H "Content-Type: application/json" \
  -d '{"query": "Explain quantum computing in simple terms"}'
```

## Key Features

- **Fast response** — The generate endpoint returns in ~300ms. Video generation happens in the background.
- **Built-in caching** — Repeated queries return cached results instantly (60-80% cache hit rate).
- **Real-time streaming** — SSE endpoint provides live progress updates during generation.
- **Multi-language** — Supports English and Hindi narration.
- **Flexible output** — Choose landscape (16:9) or portrait (9:16) orientation and 1-3 minute durations.

## Next Steps

- [Getting Started](/getting-started) — Set up your API key and make your first request.
- [REST API Reference](/api/rest) — Full endpoint documentation with examples.
